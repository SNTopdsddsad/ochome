# 原型检查记录

日期：2026-09-09。对象：独立 HTML 原型；不代表生产 Flutter 页面已完成改造。

## 已执行

- JavaScript：`node --check design/backup-restore/app.js` 通过。
- Chrome：320 / 375 / 414 / 768px × 深浅色 × 12 种页面状态，共 96 组。检查根节点与可见内容边界、44px 操作热区、恢复确认默认焦点，未发现失败。另目测四种宽度的代表界面截图。
- 总览页：320 / 375 / 414 / 768 / 1280 / 1920px，无横向滚动、iframe 越界或链接换行。
- 点击流程：一次点击启动备份、安全取消、等待云端确认后才成功、完成后没有常驻报告、恢复先准备、检查完成才确认、取消确认不激活、明确确认才激活、恢复完成回首页，共 9 项通过。
- 键盘：最终确认默认聚焦“暂不恢复”；Tab 和 Shift+Tab 在弹窗内循环；Esc 关闭后仍为待确认状态，并将焦点归还“继续恢复”。
- 对比度：浅色正文 13.90:1，辅助字在纸白上 5.11:1、总览背景上 4.60:1，主按钮 5.17:1。深色正文 14.93:1、辅助字 6.81:1、主按钮 5.19:1。正文组合均超过 4.5:1，图标与焦点超过 3:1。失效控件单独呈现禁用状态。
- CSS：颜色与字体由 tokens.css 统一提供；无渐变、假设备外框、外部字体、第三方依赖；所有 SVG 有 aria-hidden；根节点使用 overflow-x: clip；动画只移动不确定进度条并响应 reduced motion。

## Hallmark 复核

已读取并按 `references/slop-test.md` 检查。自评：Philosophy 5 / Hierarchy 5 / Execution 4 / Specificity 5 / Restraint 5 / Variety 4。

适用项通过：信息层级、无冗余卡片、真实示例来源、颜色/字体 token、对比度、克制反馈、焦点与操作状态、小屏内容边界。不宣称“58/58”：本次是已有原生工具页面的提案，网站 hero、营销 footer、展示字体轮换、长文行长、媒体 enrichment 等条件不适用。

明确保留的应用特征：系统中文字体、原生导航栏、两行备份列表项、便于触控的全宽主按钮。它们服务于现有 App 的一致性和可用性；列表的主标签没有意外折行，第二行是有意设置的来源信息。

## 限制与后续验收

所有操作仅在内存中模拟，耗时不代表真实 iCloud 性能。没有读取或修改用户数据。

真实取消边界、较大系统文字、VoiceOver、恢复中断、任务持久化、故障清理与设备表现，需在 Flutter 落地后按 plan.md 验收。本轮没有运行 Flutter 测试或重新部署 App，因为没有修改生产代码。

## Flutter 落地验证（2026-09-09）

- 备份首页与详情行为测试共 21 项通过：包含一键开始、冻结目录、完成记录隐藏、失败重试入口、清理问题、瞬时刷新失败保留目录、路由重建间隙的恢复完成反馈、不同备份任务隔离、子路由中的待确认事件、激活禁止返回，以及 320px 深浅色大字布局。
- 备份核心、存储与数据库 provider 回归共 64 项通过。
- 定向 Flutter analyze 覆盖两页面、两共享组件、完成反馈 provider 与两份页面测试，无问题。
- 完成反馈使用应用级、仅内存的单次事件，避免真实恢复引起路由重建时丢失成功提示；不重放旧任务日志。
- 真实资料的恢复不会作为 UI 验证自动执行。现有数据协议、完整性校验、数据切换和清理边界保持不变。

- iOS debug 编译成功，已更新并启动连接的 iPhone；未执行会替换用户真实资料的恢复操作。

## 文件分类汇总验证（2026-09-09）

- 首页与详情页面测试共 23 项通过；新增 1,003 个文件在 320px、1.8 倍文字、深色模式下的分类汇总，以及空文件状态。
- 覆盖全局共享文件只计一次、角色详情按归属汇总、不显示逐条文件名和文件分页。
- 定向 Flutter analyze 覆盖详情页面与两份页面测试，无问题。
