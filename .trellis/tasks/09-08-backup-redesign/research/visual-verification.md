# 全链路可视化原型验证

2026-09-08。原型是方案审核用示例，不接真实数据或 iCloud，未修改 Flutter/native 业务代码。

原型路径：`/Users/xuwudi/.codex/visualizations/2026/09/08/01a07ec2-3885-7120-b61a-7dc6c93cdc12/backup-journey.html`。

## 已验证

- JavaScript `node --check` 通过；HTML fragment 本地数据检查通过，体积约 34 KB。
- 浏览器走通：总览 → 备份前范围 → 准备/上传 → 等待云端确认 → 备份完成报告。
- 历史备份详情展示角色数、修订数、属性数和原文件数；文件清单第一页/第二页实际可切换，名称和类型清楚。
- 恢复走通：备份详情 → 范围与空间 → 下载 → 校验 → 等待用户确认 → 切换 → 恢复完成报告。
- 切换时返回入口禁用，没有错误地展示“可以离开”；校验完成没有自动覆盖。
- 360px 浅色完成页、320px 浅色恢复确认页、默认宽度深色总览已截图检查。320px 视口内 fragment document/client 宽均为 288px，无横向溢出。
- 恢复确认页窄屏标签精简；按原文件字节累计计算示例当前文件及已传输文件数，避免把文件数量进度当字节进度。
- 最终页面运行无 console error；浏览器临时视口和配色模拟已重置，临时验证页已关闭。
- 原型数据：8 个角色，46 条修订，27 个属性；8 个封面、48 图片、12 视频、10 音频、8 文档，共 86 个原文件，媒体 3,000,000,000 bytes；数据库 2,000,000 bytes。上传示例包含 6 个新媒体 + 数据库，共 425,500,000 bytes。

## 限制

失败/重试等辅助场景已提供预览状态切换，但本轮未逐一通过真实浏览器覆盖所有组合；完整 V01–V12 验收留待 Flutter/native 实施。示例传输动画、固定时间和空间不代表实际设备测量。没有验证真实 iCloud 字节遥测、时间回执同步、断网重试或数 GB 文件性能。

交互与后端数据来源合同见 `../visual-experience.md`。用户后续已确认账户合计 3 份云端备份、本机 1 份恢复前副本和严格完整恢复。用户后续已明确取消手动文件备份导出/导入；原型没有这些入口，无需改动。


账户保留决定确认后：原型总览文案已改为“账户合计保留最近 3 份”；本次只改文字，JavaScript 语法复核通过，没有重新执行完整浏览器流程。
