# Execution

1. Implement rename across repository, UI and affected fakes; add focused behavior tests.
2. Run analysis and relevant asset/backup tests, then independent spec-aware review.
3. Record the verified contract and product exploration; address review findings.
4. Rebuild/relaunch the connected iPhone in profile mode if available, retaining existing app data.

No unrelated features, dependency changes or automatic commits. Existing `ios/Podfile.lock` and `devtools_options.yaml` changes predate this task.
